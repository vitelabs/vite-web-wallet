/**
 * Eg:
 *
 * startImport: {
 *      alias: '/index'
 * }
 */

module.exports = {
    pagesAlia: 'webPages',
    startLogin: { alias: '/start' },
    tradeCenter: { alias: [ '/index', '/trade' ] },
    walletQuota: { alias: '/wallet' }
};
