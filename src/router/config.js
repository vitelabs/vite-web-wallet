/**
 * Eg:
 *
 * startImport: {
 *      alias: '/index'
 * }
 */

module.exports = {
    // [TODO] Hide trade
    // tradeCenter: { alias: [ '/index', '/trade' ] },
    assets: { alias: '/index' },
    walletQuota: { alias: '/wallet' }
};
